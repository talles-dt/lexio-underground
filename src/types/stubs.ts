// Stub for missing @/types exports

export type PillarScores = Record<string, number>;
export type MapNode = {
  id: string;
  pillar: string;
  stage: string;
};
export type MaturityStage =
  | "roots"
  | "sprouts"
  | "branches"
  | "canopy"
  | "underground";
